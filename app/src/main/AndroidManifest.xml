<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dev.kitchenrecipes">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KitchenRecipes"
        tools:targetApi="31">
        <activity
            android:name=".data.activities.RecipesListActivity"
            android:exported="false" />
        <activity
            android:name=".data.activities.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".data.activities.RecipeDetailActivity"
            android:exported="false" />
        <activity
            android:name=".data.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>